interface:
  display_name: "PG Task Orchestrator"
  short_description: "Split BE/FE and drive delivery"
  default_prompt: "Use $pg-task-orchestrator to split LIB-API-001 tasks across BE/FE, execute by gates, and produce a delivery summary."
